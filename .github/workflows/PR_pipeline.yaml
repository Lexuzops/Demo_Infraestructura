name: CI

on:
  pull_request:
    branches:
      - main

jobs:
  tests-and-lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: leer ruta actual
        run: |
          pwd
          ls
          pip --version
      # - name: Install dependencies
      #   run: |
      #     pip install pytest flake8

      # - name: Run tests
      #   run: |
      #     pytest

      # - name: Run flake8
      #   run: |
      #     source .venv/bin/activate
      #     flake8 app tests